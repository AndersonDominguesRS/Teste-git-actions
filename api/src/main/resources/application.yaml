spring:
  datasource:
    url: jdbc:postgresql://db-rickmorty:5432/rickmorty
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none

server:
  port: 8080
